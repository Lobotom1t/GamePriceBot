[phases.setup]
nixPkgs = ["python311", "chromium", "nss", "nspr", "atk", "cups", "libdrm", "dbus", "libXcomposite", "libXdamage", "libXrandr", "libgbm", "libxkbcommon", "pango", "alsa-lib"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "playwright install chromium"
]

[start]
cmd = "python bot.py"
